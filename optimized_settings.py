"""
Optimized Trading Settings for Better Order Execution
====================================================

هذا الملف يحتوي على إعدادات محسنة لتقليل إلغاء الأوامر وزيادة معدل التنفيذ
"""

# ===== إعدادات تحسين تنفيذ الأوامر =====

# استخدام أوامر السوق للبيع السريع (بدلاً من أوامر محددة السعر)
use_market_orders_for_sell = True

# مهلة زمنية لإلغاء الأوامر القديمة (بالدقائق)
order_timeout_minutes = 5

# الحد الأدنى للفترة الزمنية بين الأوامر لنفس الرمز (بالثواني)
min_order_interval_seconds = 120  # دقيقتان

# الحد الأقصى لانحراف السعر للأوامر محددة السعر (%)
max_price_deviation_pct = 0.5

# ===== إعدادات المؤشرات الفنية المحسنة =====

# تقليل حساسية المؤشرات لتقليل الإشارات المتكررة
rsi_period = 21  # زيادة من 14 إلى 21
macd_fast = 15   # زيادة من 12 إلى 15
macd_slow = 30   # زيادة من 26 إلى 30
macd_signal = 12 # زيادة من 9 إلى 12

# عتبات RSI محسنة
rsi_oversold = 25    # تقليل من 30 إلى 25
rsi_overbought = 75  # زيادة من 70 إلى 75

# ===== إعدادات إدارة المخاطر =====

# نسبة الموقف من المحفظة
max_position_percentage = 0.02  # تقليل من 3% إلى 2%

# نسبة وقف الخسارة
stop_loss_percentage = 0.02  # زيادة من 1.5% إلى 2%

# نسبة جني الأرباح
take_profit_percentage = 0.008  # زيادة من 0.1% إلى 0.8%

# الحد الأقصى للصفقات اليومية
max_daily_trades = 6  # تقليل من 8 إلى 6

# ===== إعدادات فلترة الأسهم =====

# الحد الأدنى لحجم التداول اليومي
min_daily_volume = 100000

# الحد الأدنى لسعر السهم
min_stock_price = 5.0

# الحد الأقصى لسعر السهم
max_stock_price = 500.0

# ===== إعدادات التوقيت =====

# فترة التحقق من الإشارات (بالثواني)
signal_check_interval = 30  # زيادة من 10 إلى 30 ثانية

# فترة التحقق من الأوامر المعلقة (بالثواني)
order_check_interval = 60  # زيادة من 30 إلى 60 ثانية

# ===== إعدادات السوق =====

# أوقات التداول
market_open_time = "09:30"
market_close_time = "15:30"  # إغلاق مبكر لتجنب تقلبات نهاية اليوم

# تجنب التداول في أول وآخر 30 دقيقة
avoid_first_minutes = 30
avoid_last_minutes = 30

# ===== إعدادات التسجيل =====

# مستوى التسجيل
log_level = "INFO"

# حفظ سجلات مفصلة للأوامر
detailed_order_logging = True

# ===== إعدادات الأمان =====

# الحد الأقصى للخسارة اليومية (%)
max_daily_loss_percentage = 5.0

# الحد الأقصى للخسارة الإجمالية (%)
max_total_loss_percentage = 10.0

# إيقاف التداول عند الوصول للحد الأقصى للخسارة
stop_trading_on_max_loss = True

# ===== إعدادات التحسين =====

# استخدام ذاكرة التخزين المؤقت للبيانات
use_data_caching = True

# فترة انتهاء صلاحية البيانات المخزنة (بالثواني)
cache_expiry_seconds = 60

# استخدام معالجة متوازية للرموز
use_parallel_processing = True

# عدد الخيوط للمعالجة المتوازية
max_worker_threads = 4

print("✅ تم تحميل الإعدادات المحسنة بنجاح!")
print("📈 هذه الإعدادات مصممة لتقليل إلغاء الأوامر وزيادة معدل التنفيذ")